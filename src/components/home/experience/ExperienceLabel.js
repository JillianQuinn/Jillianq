import React from 'react';

import "./ExperienceLabel.css"

const ExperienceLabel = ( {label } ) => {
    return (
        <label className="experience-label-minimal">{label}</label >    
    );
}

export default ExperienceLabel;